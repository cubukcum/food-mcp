version: '3.8'

services:
  food-mcp:
    build: .
    ports:
      - "8001:8001"
    environment:
      - API_URL=http://localhost:5000
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
